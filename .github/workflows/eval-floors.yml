# Pixel golden floors + Hub pin lockstep. No model weights, no Metal generate.
name: Eval floors

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: eval-floors-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  floors:
    name: Golden metric floors
    runs-on: macos-26
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # mlx-swift 0.31.6 is tools-version 6.3. macos-26 default is Xcode 26.6.
      - name: Select Xcode 26.6
        run: sudo xcode-select -s /Applications/Xcode_26.6.app/Contents/Developer

      - name: Cache SPM
        uses: actions/cache@v4
        with:
          path: |
            .build
            ~/Library/Caches/org.swift.swiftpm
          key: eval-floors-macos26-xcode26.6-${{ hashFiles('Package.resolved', 'Package.swift') }}
          restore-keys: |
            eval-floors-macos26-xcode26.6-

      - name: Hub pins + eval floors
        run: ./Scripts/ci-eval-floors.sh
